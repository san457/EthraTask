{
  "name": "ethratask",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspace=backend"
  }
}
